[project]
name = "multi-agent-simple"
version = "0.1.0"
description = "Streaming multi-agent demo with FastAPI and OpenAI Agents"
requires-python = ">=3.12,<3.13"
dependencies = [
  "fastapi",
  "openai-agents",
  "python-dotenv",
  "uvicorn",
]

[tool.uv]
package = false
